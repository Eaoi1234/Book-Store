module.exports = {
    darkMode: 'class', // Enable dark mode using the 'class' strategy
    theme: {
      extend: {
        colors: {
          background: 'var(--background-color)',
          text: 'var(--text-color)',
        },
      },
    },
    variants: {
      extend: {},
    },
    plugins: [],
  }